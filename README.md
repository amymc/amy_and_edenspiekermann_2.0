# amy_and_edenspiekermann_2.0

React es6 app with React Router<br/>
Displays photos from the Flickr API tagged with edenspiekermann

To run locally<br/>
`npm install`<br/>
`npm run start`

Click on tag or author to filter list by that parameter
